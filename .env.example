# Password for /admin/login. Change this before deploying.
ADMIN_PASSWORD=kairos-admin

# Secret used to sign session cookies (admin + gallery access).
# Generate a real one before deploying, e.g.: openssl rand -hex 32
SESSION_SECRET=change-this-to-a-long-random-string
